# Frontend - Proyecto Servicios

Instalación:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
```

En desarrollo frontend se conecta al backend en `http://localhost:8000`.
